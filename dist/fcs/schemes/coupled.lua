local a=require("fcs.schemes.level_flight")local b={}b.__index=b;function b.new(c,d)return setmetatable({inner=a.new(c),decoupled=d and d.decoupled or false},b)end;function b:reset()self.inner:reset()end;function b:update(e,f,g,h)local i=self.inner:update(e,f,g,h)if e.yawRear then i.yawRear=i.yaw;i.yaw=0 end;if e.surgeActive then i.surge=e.surgeCmd or 0 elseif self.decoupled then i.surge=0 end;if e.swayActive then i.sway=e.swayCmd or 0 elseif self.decoupled then i.sway=0 end;return i end;return b