local a=require("fcs.schemes.level_flight")local b={}b.__index=b;function b.new(c,d)return setmetatable({inner=a.new(c),decoupled=d and d.decoupled or false},b)end;function b:reset()self.inner:reset()end;function b:update(e,f,g,h,i)local j=self.inner:update(e,f,g,h,i)if e.yawRear then j.yawRear=j.yaw;j.yaw=0 end;if e.surgeActive then j.surge=e.surgeCmd or 0 elseif self.decoupled then j.surge=0 end;if e.swayActive then j.sway=e.swayCmd or 0 elseif self.decoupled then j.sway=0 end;return j end;return b