local a=require("fcs.comms.cfgsync")local b=require("fcs.io.cfgspec")local c={}c.__index=c;local d={}function d.new(e)e=e or{}return setmetatable({read=e.read,dir=e.dir or"/",_running=false,_lastSeen=nil},c)end;function c:start()self._running=true end;function c:stop()self._running=false end;function c:running()return self._running end;function c:onMessage(f)if type(f)=="table"and(f.k=="hello"or f.k=="req")then self._lastSeen=os.epoch("utc")end;if not self._running then return nil end;local g=function(h)local i=b.FILES[h]if not i then return nil end;return self.read(self.dir..i)end;return a.Responder.decide(f,g)end;function c:status()return{running=self._running,lastSeen=self._lastSeen}end;return d