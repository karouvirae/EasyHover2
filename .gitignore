# Claude Code / dev tooling
.claude/
.mcp.json

# Node build tooling (dist/ is the committed artifact; node_modules is dev-only)
node_modules/

# Milestone backups (kept locally, not versioned; git history serves this now)
backup/

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini

# SDD scratch (ledger, briefs, review packages)
.superpowers/

# tools/run_gen.sh's scratch CraftOS-PC data dir (regenerated every run)
tests/.craftos/
